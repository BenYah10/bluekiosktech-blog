backend:
  name: github
  branch: main
  repo: BenYah10/bluekiosktech-blog
  base_url: https://www.bluekiosktech.blog
  auth_endpoint: /api/oauth
  # (optionnel) si vous utilisez un domaine custom pointant sur Vercel :
  site_domain: www.bluekiosktech.blog

publish_mode: editorial_workflow

media_folder: "assets/uploads"
public_folder: "/assets/uploads"

slug:
  clean_accents: true
  encoding: "unicode"
  sanitize_replacement: "-"

i18n:
  structure: multiple_folders   # => content/posts/fr/... et content/posts/en/...
  locales: [fr, en]
  default_locale: fr

editor:
  preview: true

collections:
  # ────────────────────────────────────────────────────────────────────────────
  # ARTICLES (Markdown, multi-langue, arborescence année/mois/slug)
  # ────────────────────────────────────────────────────────────────────────────
  - name: "posts"
    label: "Articles"
    label_singular: "Article"
    folder: "content/posts"
    create: true
    i18n: true
    format: "frontmatter"
    extension: "md"
    # path à l’intérieur du dossier de langue (fr | en)
    path: "{{year}}/{{month}}/{{slug}}"
    preview_path: "post.html?locale={{locale}}&slug={{slug}}"
    sortable_fields: ["date", "title"]
    view_filters:
      - label: "FR"
        field: "lang"
        pattern: "fr"
      - label: "EN"
        field: "lang"
        pattern: "en"
    fields:
      # meta technique — stockée pour faciliter des filtres si besoin
      - { label: "Langue (auto)", name: "lang", widget: "hidden", i18n: duplicate, default: "{{locale}}" }

      - { label: "Titre", name: "title", widget: "string", i18n: true }
      - { label: "Slug", name: "slug", widget: "string", i18n: duplicate, hint: "Sans espaces, utilisé dans l’URL" }
      - { label: "Date", name: "date", widget: "datetime", i18n: duplicate }

      - label: "Auteur"
        name: "author"
        widget: "relation"
        collection: "authors"
        search_fields: ["name"]
        value_field: "name"
        display_fields: ["name"]
        i18n: duplicate

      - { label: "Chapo / Résumé", name: "excerpt", widget: "text", i18n: true, required: false, hint: "Résumé court (meta description)" }

      - label: "Image à la une"
        name: "cover"
        widget: "image"
        i18n: duplicate
        allow_multiple: false
        required: false

      - label: "Mots-clés"
        name: "tags"
        widget: "list"
        i18n: translate
        required: false

      - { label: "Contenu", name: "body", widget: "markdown", i18n: translate }

  # ────────────────────────────────────────────────────────────────────────────
  # AUTEURS (JSON, mono-langue) : content/authors/*.json
  # ────────────────────────────────────────────────────────────────────────────
  - name: "authors"
    label: "Auteurs"
    label_singular: "Auteur"
    folder: "content/authors"
    create: true
    format: "json"
    extension: "json"
    i18n: false
    identifier_field: "name"
    slug: "{{slug}}"
    fields:
      - { label: "Nom", name: "name", widget: "string" }
      - { label: "Rôle / Fonction", name: "role", widget: "string", required: false }
      - { label: "Bio courte", name: "bio", widget: "text", required: false }
      - { label: "Avatar", name: "avatar", widget: "image", required: false }
      - { label: "Email", name: "email", widget: "string", required: false }
      - label: "Réseaux"
        name: "social"
        widget: "object"
        collapsed: true
        required: false
        fields:
          - { label: "LinkedIn", name: "linkedin", widget: "string", required: false }
          - { label: "X / Twitter", name: "twitter", widget: "string", required: false }
          - { label: "Site perso", name: "website", widget: "string", required: false }

  # ────────────────────────────────────────────────────────────────────────────
  # RÉGLAGES DU SITE (JSON, multi-langue pour les textes)
  # ────────────────────────────────────────────────────────────────────────────
  - name: "settings"
    label: "Réglages"
    i18n: true
    files:
      - name: "site"
        label: "Paramètres généraux"
        file: "content/settings/site.json"
        format: "json"
        fields:
          - { label: "Nom du site", name: "site_name", widget: "string", i18n: true }
          - { label: "Baseline", name: "tagline", widget: "string", i18n: true, required: false }
          - { label: "Description", name: "description", widget: "text", i18n: true, required: false }

          - label: "Réseaux"
            name: "socials"
            widget: "object"
            collapsed: true
            i18n: duplicate
            fields:
              - { label: "Facebook", name: "facebook", widget: "string", required: false }
              - { label: "Instagram", name: "instagram", widget: "string", required: false }
              - { label: "LinkedIn", name: "linkedin", widget: "string", required: false }
              - { label: "YouTube", name: "youtube", widget: "string", required: false }

          - label: "Apparence"
            name: "theme"
            widget: "object"
            i18n: duplicate
            collapsed: true
            fields:
              - label: "Mode"
                name: "mode"
                widget: "select"
                options:
                  - { label: "Système", value: "system" }
                  - { label: "Clair", value: "light" }
                  - { label: "Sombre", value: "dark" }
                default: "system"
                required: false
              - label: "Couleur de marque"
                name: "brand_color"
                widget: "color"
                required: false
                i18n: duplicate

# Fin du fichier
